{% from "../button/button.nunj" import button %}

{% macro productCard(props) %}
	{% set activeClassName = '' %}
	{% if props.inWishlist %}
		{% set activeClassName = ' _active' %}
	{% endif %}
	<article class="{% if props.className %}{{ props.className }} {% endif %}product-card">
		<a class="product-card__title ttu" href="{{ props.url }}">{{ props.title }}</a>
		<p class="product-card__brand p3">{{ props.brand }}</p>
		<p class="product-card__price p3">{{ props.price }}</p>
		{{ button({
			className: 'product-card__button _wishlist' + activeClassName,
			attr: 'aria-label="Добавить в вишлист"'
		}) }}
		<a class="product-card__link-photo" href="{{ props.url }}">
			<img class="product-card__photo" src="{{ props.photo }}.png" srcset="{{ props.photo }}@2x.png 2x" width="304" height="364" alt="">
			<img class="product-card__photo _zoom" src="{{ props.zoomPhoto }}.png" srcset="{{ props.zoomPhoto }}@2x.png 2x" width="432" height="540" alt="">
		</a>
		{{ button({
			text: 'В корзину',
			className: 'product-card__button _product-card',
			attr: 'aria-label="В корзину"'
		}) }}
		<div class="product-card__badges">
			{% if props.new %}
				<p class="product-card__badge _new ttu">New</p>
			{% endif %}
			{% if props.discount %}
				<p class="product-card__badge _discount">{{ props.discount }}</p>
			{% endif %}
			{% if props.featureImg %}
				<img
					class="product-card__badge _feature"
					src="{{ props.featureImg }}.png"
					width="48"
					height="48"
					alt="{% if props.featureImgAlt %}{{ props.featureImgAlt }}{% endif %}"
				>
			{% endif %}
		</div>
	</article>
{% endmacro %}
